[project]
name = "run-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sebastian Korotkiewicz", email = "skorotkiewicz@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "prompt-toolkit>=3.0.53",
]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
